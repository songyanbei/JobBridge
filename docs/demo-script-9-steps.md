# JobBridge 甲方 Demo 9 步演示脚本

> 数据准备：先跑 `scripts/reset_and_seed_data.py` 灌底层种子，再跑 `scripts/demo_seed_supplement.sql` 补剧本卡位（已就绪）。

## 演示前 5 分钟自检清单

- [ ] 后端 `uvicorn` 已起，`/health` 返回 ok
- [ ] Worker 进程已起（消费 Redis 队列）
- [ ] 如演示 Phase5 推荐体验：确认 app 与 worker 使用同一套推荐体验环境变量，并已在调整后重启
- [ ] 前端运营后台 admin 已登录（admin/admin123）
- [ ] 企微 webhook（或 mock 工具）能发消息到后端
- [ ] 演示用账号确认：
  - 厂家：`seed_factory_014`（苏州永利塑胶 · 朱艳）
  - 中介：`seed_broker_002`（云国华）
  - 工人：用一个**新的** external_userid（如 `demo_worker_show_001`）触发自动注册
- [ ] 苏州电子厂岗位密度：10 条（演示前 SQL 复核：`SELECT COUNT(*) FROM job WHERE city='苏州市' AND job_category='电子厂'`）
- [ ] 命中匹配依据 gate 时，回复里会出现"匹配依据"；未命中时不出现该文案

---

## 全流程一览

| # | 角色 | 一句话 | 看后台 |
|---|------|--------|--------|
| 1 | 厂家 | 一句话发布完整岗位 | 岗位管理 |
| 2 | 厂家 | 信息不全 → AI 多轮追问补全 | 岗位管理 |
| 3 | 厂家 | 含 high 敏感词 → 自动驳回 | 岗位管理 / 审核日志 |
| 4 | 中介 | 帮工人找岗位 | — |
| 5 | 工人 | 完整描述 → Top3 推荐 | — |
| 6 | 工人 | "工资再高点 7000 以上" | 强调字段权限脱敏 |
| 7 | 工人 | "再来几个" | 翻页（复用快照） |
| 8 | 工人 | "至少 9500" → 薪资区间直接命中 | **核心亮点** |
| 9 | 运营 | 数据看板 fly-by | 收尾 |

---

## 第 1 步 · 厂家完整发布

**身份**：厂家 · 朱艳（苏州永利塑胶）

**用户消息**：
```
苏州工业园区招20个电子厂普工，男女不限，5500-7000，包吃住，两班倒，长期工
```

**预期回复要点**：
> "您的岗位信息已入库，将进入匹配池。"

**演示动作 + 口播**：
- 切到运营后台 → **岗位管理**，按"最近创建"排序，点开刚发布的那条
- 展开**详情**：城市/工种/薪资/headcount/包吃住/班次/审核状态全都从一句自然语言抽出来了
- **口播**："厂家不需要填表单，一句话发出去，模型就把 10+ 个结构化字段全抽出来了，状态默认是 passed 直接上架"

---

## 第 2 步 · 厂家信息不全 → 多轮追问

**身份**：厂家 · 朱艳（同上）

**第 1 轮发**：
```
我想发个上海的服装厂岗位
```

**预期系统反问要点**（缺薪资 + 招几人，具体措辞由 schema 模板动态生成）：
> 需要补充薪资和招聘人数。

**第 2 轮发**：
```
月薪6000-7500，招10个人，包吃住
```

**预期回复**：
> "您的岗位信息已入库，将进入匹配池。"

**演示动作 + 口播**：
- 后台 **岗位管理** 看到这条新增
- **口播**："注意——这两条消息是分开发的。系统在 30 分钟会话窗内把它们拼成一个岗位入库。多轮上下文是个状态机，不是简单的字符串拼接"

---

## 第 3 步 · 含 high 敏感词 → 自动驳回

**身份**：厂家 · 朱艳（同上）

**用户消息**：
```
苏州招10个网管，月薪5000，要2个人
另外可以代开发票

```

**预期回复**：
> "您的岗位信息已收到，但未通过内容审核。如有疑问请联系客服。"
（**不会**直接上架）

**演示动作 + 口播**：
- 后台 → **岗位管理** → 看到这条 `audit_status=rejected`
- 点开看：审核原因带上"代开发票"敏感词标记
- 如需看流水，后台审核日志里会有自动驳回记录
- **口播**："当前演示的是 high 级敏感词自动驳回。`代开发票` 在种子词表里是 high，所以不会进入人工待审队列；LLM 安全检查目前只是预留接入点，真正生效的是敏感词字典规则"

---

## 第 4 步 · 中介帮工人找岗位

**身份**：中介 · 云国华

**用户消息**：
```
帮一个工人找苏州电子厂的活，5500以上，包吃住
```

**预期回复**：
返回苏州电子厂 Top3 岗位；当前前台岗位结果格式只展示公司、工种、薪资、地点和班次，不渲染岗位联系人电话：
> 为您找到 3 个匹配岗位：
> ① 苏州永利塑胶 | 电子厂
>    5500-7200 元/月（包吃包住）
>    苏州市工业园区
>    两班倒
> ② ……
> ③ ……

若 `RECOMMENDATION_EXPERIENCE_ENABLED=true` 且匹配依据 rollout 命中，每条结果最多额外出现 2 条"匹配依据"，只说明地点、工种、薪资、包吃住等可验证命中，不解释最终排序。

**口播**：
- "中介这个角色比较特殊：双向权限——既能发岗位又能查岗位查工人。这条消息触发了 `search_job` 意图"
- "这里演示的是中介有权限查岗位；联系人和电话不会出现在这条前台岗位回复里，字段展示以当前格式化器为准"

---
## 第 5 步 · 工人完整描述 → Top3 推荐

**身份**：工人 · `demo_worker_show_001`（新用户，首次发消息会自动注册）

**用户消息**：
```
苏州找电子厂，5500以上，包吃住
```

**预期回复**（Top3，**不带联系方式**）：
> 为您找到 3 个匹配岗位：
> ① 苏州永利塑胶 | 电子厂
>    5500-7200 元/月（包吃包住）
>    苏州市工业园区
>    两班倒
> ② 对应厂家公司名 | 电子厂
>    6500-8500 元/月（包吃包住）
>    苏州市高新区
> ③ 苏州永利塑胶 | 电子厂
>    7000-9000 元/月（包吃包住）
>    苏州市昆山市
>
> 如还有未展示候选，回复中会显示实际剩余岗位数量，并提示回复"更多"继续查看
> 不满意？直接告诉我调整方向，比如"薪资再高点""要包住的"

**口播**：
- "工人看到的是岗位基础信息，**没有联系方式**——当前岗位前台格式不展示联系人电话"
- "背后是三步漏斗：先 SQL 硬过滤、再 LLM 语义重排、最后按角色权限过滤字段；但当前回复模板不会把重排理由逐条渲染出来"

---

## 第 6 步 · "工资再高点" → 多轮修改

**用户消息**（同一会话，紧接上一步）：
```
工资再高点，7000以上
```

**预期回复**：
返回新的 Top3，薪资区间都能覆盖 7000：
> 为您找到 3 个匹配岗位：
> ① 对应厂家公司名 | 电子厂
>    6500-8500 元/月（包吃包住）
>    苏州市高新区
>    白班+小夜班
> ② 苏州永利塑胶 | 电子厂
>    7000-9000 元/月（包吃包住）
>    苏州市昆山市
>    两班倒
> ③ 对应厂家公司名 | 电子厂
>    7500-9800 元/月（包吃包住）
>    苏州市吴中区
>    白班
>
> 如还有未展示候选，回复中会显示实际剩余岗位数量，并提示回复"更多"继续查看

**口播**：
- "我没有重发城市和工种，只说'工资再高点'。模型在多轮上下文里把上一轮 criteria 累积下来，只更新薪资字段"
- "前一轮的 city=苏州市 / job_category=电子厂 自动保留了——这是多轮对话状态机的核心"
- "再次注意：仍然没有联系方式"

---

## 第 7 步 · "再来几个" → 翻页

**用户消息**：
```
再来几个
```

**预期回复**：
返回上一次结果中没展示过的下一批岗位；show_more 最多返回 3 条，是否还有下一页以回复提示为准。

**口播**：
- "这条命令叫 show_more。系统识别后**不会重新跑 LLM 重排**，而是从上一轮的快照里取下一批——省成本、低延迟"
- "比如刚才 7000+ 的候选池超过 Top3，这次会从快照返回下一批；实际剩余数量以系统回复为准"

---

## 第 8 步 · "至少 9500" → 薪资区间直接命中（核心亮点）

**用户消息**：
```
工资再高点，至少9500
```

**预期回复**：
> 为您找到 3 个匹配岗位：
> ① 苏州永利塑胶 | 电子厂
>    8600-11000 元/月（包吃包住）
>    苏州市工业园区
>    白班
> ② 对应厂家公司名 | 电子厂
>    8800-11500 元/月（包吃包住）
>    苏州市相城区
>    白班
> ③ 苏州永利塑胶 | 电子厂
>    9000-12000 元/月（包吃包住）
>    苏州市工业园区
>    白班

**口播（这一步建议慢慢讲）**：
- "用户提了个更高的条件——苏州电子厂 9500 起。系统不会只看岗位下限，而是看岗位薪资区间能不能覆盖 9500"
- "比如 8600-11000 的岗位，下限低于 9500，但上限覆盖 9500，所以这是符合用户要求的，不应该被错误过滤掉"
- "这一步展示的是搜索语义的准确性：只有真实低召回时才需要自动放宽，不能让 fallback 掩盖过滤 bug"
- "如果要演示自动放宽，使用下方 Phase 5 验收补充中的固定输入和 seed，不要临时改线上岗位"

---

## Phase 5 验收补充

### 1. 固定 seed 与完整服务烟测

先完成基础 seed，再追加 Phase 5 补充 seed。烟测必须提供 `--mysql-dsn`，否则不允许判定成功：

```bash
docker exec -i jobbridge-mysql mysql -u jobbridge -pjobbridge jobbridge < scripts/demo_seed_supplement.sql

backend/.venv/Scripts/python.exe scripts/demo_acceptance_smoke.py \
  --base-url http://127.0.0.1:8000 \
  --redis-url redis://127.0.0.1:6379/0 \
  --mysql-dsn mysql+pymysql://jobbridge:jobbridge@127.0.0.1:3306/jobbridge \
  --message "找苏州电子厂，工资下限 5500，最好包吃住" \
  --message "更多" \
  --expect "为您找到" \
  --reject "身份证" --reject "联系电话" --reject "详细地址"
```

脚本启动前会校验 `demo_supp_v1`、苏州电子厂补充岗位至少 7 条，以及第 5/6/8 步场景分别至少 2/2/3 条；每条入站消息都必须在 `wecom_inbound_event.status=done` 后收到至少一条出站业务回复。运行结束会清理本次用户的 session、队列消息、入站事件、对话日志和用户数据。

### 2. 自动放宽：固定输入与预期摘要

补充 seed 中的第 8 步岗位最高上限为 12000。用全新用户发送：

```text
找苏州电子厂，工资下限 12100，包吃住
```

原条件应为 0 命中，自动放宽 10% 后以 `12100 → 10890` 重新搜索并命中第 8 步岗位。开启推荐体验 gate 后，摘要必须同时包含原条件可见数、`薪资下限12100 → 10890`、放宽后可见数和本次展示数；仍为 0 命中时只出现一次放宽摘要，随后给出建议，不重复拼接放宽说明。

### 3. 分页耗尽与软偏好 notice

在第 5 步结果后连续发送 `更多`，直到候选快照耗尽。最后一次只返回收尾提示，不重复旧岗位，文案形如：

```text
本轮结果已经看完了。可以试试这些方向重新搜索：...
```

在推荐体验 master switch 开启、软偏好排序和相关 notice rollout 均为 100 时，用全新用户发送：

```text
找苏州电子厂，工资下限 5500，包吃住
```

候选确实命中 `provide_meal/provide_housing` 时，回复应包含 `已优先展示符合「包吃住」偏好的岗位`；软偏好只影响排序和该 notice，不改变硬过滤候选范围。

### 4. rollout=100/0 对照验收

在同一 seed、同一消息下分别重启 app/worker 做两轮：

```dotenv
recommendation_experience_enabled=true
post_search_policy_mode=on
phase5_rollout_percentage=100
recommendation_reason_rollout_percentage=100
recommendation_reason_shadow_enabled=false
soft_preference_ranking_enabled=true
soft_preference_ranking_rollout_percentage=100
soft_preference_reason_rollout_percentage=100
soft_preference_notice_rollout_percentage=100
```

100% 轮应看到可验证的地点/工种/薪资匹配依据和软偏好 notice。第二轮明确关闭整体二阶段策略和所有用户 rollout：

```dotenv
recommendation_experience_enabled=true
post_search_policy_mode=off
phase5_rollout_percentage=0
recommendation_reason_rollout_percentage=0
recommendation_reason_shadow_enabled=false
soft_preference_ranking_enabled=true
soft_preference_ranking_rollout_percentage=0
soft_preference_reason_rollout_percentage=0
soft_preference_notice_rollout_percentage=0
```

重启后使用同一消息：岗位结果仍应正常返回，但不应出现匹配依据或软偏好 notice。若要单独验证 shadow，将 `recommendation_reason_shadow_enabled=true` 置于 `post_search_policy_mode=shadow` 的观测轮；shadow 只写结构化日志，不能让回复出现匹配依据。

---

## 第 9 步 · 数据看板收尾

**演示动作**：
- 后台 → **首页 Dashboard**
- 扫一眼几个图表：今日新增用户 / 岗位 / 简历 / 检索量、审核积压、用户角色分布

**口播**：
- "运营老板进来一眼能看到关键指标。所有图表都从对话日志和业务表实时聚合的"
- "整套系统：底层数据 + 三方角色 + AI 多轮匹配 + 字段权限 + 审核流水线 + 运营看板，今天演示的是端到端的链路"

---

## 关键风险预案

| 风险 | 兜底 |
|------|------|
| LLM 抽取出来的 `salary_floor_monthly` 不是 5500 / 7000 / 9500 整数 | 改用更明确的措辞，例如"工资下限 9500"、"月薪 7000 以上" |
| 第 6 步 LLM 把"再高点"理解为只 +500 → 抽出 6000 | 直接说"7000 以上"，避免模糊措辞 |
| 第 7 步 show_more 没有更多结果（候选池被取空） | 演示前先在 mock 里跑一次预演确认 |
| 第 8 步没有召回 8600-11000 岗位 | 检查搜索 SQL 是否使用薪资区间语义：`ceiling >= D OR (ceiling IS NULL AND floor >= D)` |
| 数据库中文乱码 | 确认 connection 用 utf8mb4，环境变量已设置 |

## 数据回滚 / 重置

```bash
# 清空苏州电子厂剧本卡位（保留底层 reset_and_seed 数据）
mysql -u jobbridge -pjobbridge jobbridge -e \
  "DELETE FROM job WHERE JSON_EXTRACT(extra, '\$.demo_tag') = 'demo_supp_v1'"

# 完全重置 + 重新灌
backend/.venv/Scripts/python.exe scripts/reset_and_seed_data.py --yes
backend/.venv/Scripts/python.exe scripts/_apply_demo_supp.py
```
