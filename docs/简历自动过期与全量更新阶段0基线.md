# 简历自动过期与全量更新阶段 0 基线

## 1. 代码与文档基线

| 项目 | 值 |
|---|---|
| 分支起点 | 本地 `main@64bbc07917bcff4fb38301c1404059c45d08c409` |
| 开发分支 | `codex/resume-expiry-full-update-v01` |
| 原方案目标路径 | `docs/简历自动过期与全量更新实施方案_v0.1.md` |
| 原方案 SHA-256 | `BB18C3ECD7BE349F9E2CB154D3F990E80C4EA93A2DB8CE197C55A29011320F96` |
| 原方案字节数 | `89918` |
| 文本编码 | UTF-8，无 BOM，LF |

阶段 0 只冻结契约、记录基线和提供只读预检，不修改业务代码、schema 或运行时配置。

## 2. 回归基线

### 2.1 后端单元测试

- 独立进程 1 执行媒体预检单测：`6 passed`；
- 独立进程 2 执行其余 unit 全集：`2099 passed`、`53 warnings`，耗时 `21.17s`；
- 两个进程合计：`2105 passed`、`0 skipped`、`0 failed`。采用双进程是为了避免媒体预检测试设施对进程级全局状态造成污染；两批用例互不重叠，合并结果等价于完整 unit 全集。

### 2.2 隔离集成测试

集成环境使用阶段专属 MySQL `8.0.45` 与 Redis `7.4.10`，Redis 实际配置为 AOF 开启、`appendfsync=always`、`maxmemory-policy=noeviction`。

- Batch A：`19 passed`、`0 skipped`、`0 failed`。执行文件为 `test_db.py`、`test_search_sql_mysql.py`、`test_phase3_upload_and_search.py`、`test_phase3_delete_flow.py`、`test_phase3_upload_then_search_smoke.py`、`test_job_media_hard_delete_delay_mysql.py`、`test_media_target_cleanup_lock_order_mysql.py`；
- Batch B：`72 passed`、`0 skipped`、`0 failed`。执行文件为 `test_redis.py`、`test_target_cleanup_checkpoint_redis_mysql.py`、`test_target_cleanup_backfill_mysql.py`、`test_target_cleanup_lease_mysql.py`、`test_target_cleanup_upsert_mysql.py`、`test_ttl_outbox_delivery_lock_order_mysql.py`、`test_outbox_claim_lock_scope_mysql.py`、`test_privacy_lock_order_mysql.py`、`test_privacy_redaction_batch_mysql.py`、`test_recommendation_privacy_delete.py`、`test_recommendation_candidate_delete_scrub.py`、`test_recommendation_session_outbox_consistency.py`、`test_recommendation_worker_delivery.py`、`test_recommendation_plaintext_redaction.py`；
- 两批合计：`91 passed`、`0 skipped`、`0 failed`，覆盖数据库连接、真实 MySQL 搜索、上传/审核/搜索/删除、TTL 与媒体硬删门禁、目标清理锁与租约、Redis checkpoint、推荐隐私删除及投递一致性。

阶段专属容器、端口和数据库未复用已有开发数据；测试结束后按精确容器名称删除并复核无残留，未清理或修改既有开发容器及数据。

### 2.3 只读预检与隐私验证

- MySQL schema 依赖表检查为 `13/13`，预检 SQL 在 MySQL `8.0.45` 完整执行通过；所有可执行语句均为 `SELECT` 或 `WITH ... SELECT`；
- 媒体预检 CLI 固定输出 `25` 个聚合字段；含目标 ID、owner、URL、原始 key 和规范化 key 的 canary 均未出现在 stdout/stderr；
- 开发态 SQLAlchemy echo、绑定参数和数据库异常均未泄漏；直接 CLI `--help` 启动通过；
- 预检执行前后受检数据表哈希一致，确认 SQL 与媒体 CLI 均未写库。

### 2.4 前端基线

- `npm ci` 安装 `194` 个 package；lint 通过；生产 build 通过，共处理 `2291` 个模块，耗时 `16.37s`；
- 依赖审计报告 `10` 个既有漏洞（`3 moderate`、`7 high`）；deprecated package 为 `inflight@1.0.6`、`@humanwhocodes/config-array@0.13.0`、`rimraf@3.0.2`、`glob@7.2.3`、`@humanwhocodes/object-schema@2.0.3`、`eslint@8.57.1`。

上述漏洞和弃用提示来自基线已有依赖，阶段 0 不升级依赖。

## 3. 数据快照边界

`backend/sql/migrations/phase11_resume_preflight.sql` 仅执行只读 SQL 聚合及 JSON 形态检查；`backend/scripts/phase11_resume_preflight_media.py` 使用生产同源规范化函数完成媒体核对。两者仅输出固定字段的安全聚合计数，不输出用户标识、简历正文、手机号、媒体 URL、原始 object key、规范化 key 或目标 ID。

当前工作区不连接真实预发布或生产数据库。生产脱敏快照的预检、up/down 演练、执行计划和不变量结果均标记为“待发布方执行”，不得填入虚构的数量或通过结论。发布方执行后应归档：数据库版本、脚本 SHA-256、开始/结束时间、全部聚合结果、实例 build 探针清单和审批单号。

## 4. 阶段 0 验收

- 原方案目标文件哈希与源文件精确一致。
- 决策补充覆盖阶段依赖、脚本位置、RBAC、迁移/回退和孤儿例外规则。
- 预检 SQL 对 `main` 基线 schema 可解析，且 SQL 与媒体脚本的所有业务数据输出均为聚合计数。
- 独立测试子智能体已执行完整基线门禁并归档上述真实结果。
- 评审子智能体确认无 PII 泄漏、无业务代码变化、无生产执行痕迹。
